import { View, Text } from "react-native";
import React from "react";
import { UserOption } from "@/types";

const MatchOptions = ({ options }: { options: UserOption[] }) => {
  return (
    <View>
      <Text>MatchOptions</Text>
    </View>
  );
};

export default MatchOptions;
